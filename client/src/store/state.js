//全局状态树
// 当我们的应用程序启动时，初始状态是todos的空列表（[]），
// 它将存储我们新添加的todos和一个空的字符串，该字符串将保留添加到todos。

//存放组件之间共享的数据

export default {
   discountTime:[],
   alipayConfigEdit:[],
   garageInfoEdit:[],
   wxConfig:[],
   systemWorkPlatform:[]
}